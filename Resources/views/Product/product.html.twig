{% extends layout %}
{% block title %}
    {{ product.name }}
{% endblock %}

{% block body %}
    <h1>{{ product.name }}</h1>

    {{ product.description|raw }}

    <div class="well well-lg">
        {{ viewComponent('product-mark-favorite')|raw }}
    </div>
{% endblock body %}
